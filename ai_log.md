# Gemini 활용 기록

> 이번 수행평가에서는 학교 계정 Gemini 사용을 허용합니다.  
> 단, Gemini가 만든 코드를 이해하지 못한 채 그대로 제출하면 안 됩니다.  
> 실제 개인정보, 친구 이름, 성적, 전화번호, 주소 등은 절대 입력하지 않습니다.

---

## 1차 Gemini 활용

- 사용 날짜:1 차시
- 사용 목적:작성한 코드가 실행되지 않는 문제(오류) 해결 및 항상 같은 결과가 나오는 원인 파악
- 내가 입력한 프롬프트:main() 뭘 입력해도 항상 같은 답이 나오는데?

```text
여기에 내가 Gemini에게 입력한 질문을 붙여넣기
```

- Gemini가 제안한 내용 요약:모든 함수를 잘 만들었으나 맨 마지막에 main() 함수 호출을 누락하여 실행되지 않는다는 점을 알려줌. 또한, 항상 같은 결과가 나오는 이유는 2차원 리스트 데이터가 코드 맨 위에 고정(하드코딩)되어 있기 때문이며, 숫자를 직접 바꾸거나 input()으로 동적 입력을 받으라고 제안함.
- 내가 반영한 부분:코드의 제일 마지막에 main()을 추가하여 프로그램이 정상적으로 실행되도록 조치함.
- 내가 수정하거나 사용하지 않은 부분:매번 횟수를 input()으로 직접 질문하여 입력받도록 코드를 수정하는 뼈대(함수)도 제안받았으나, 평가 기준과 현재 프로젝트의 완성도를 고려하여 최종적으로는 미리 데이터를 작성해 두는 방식을 유지함.
- 반영 위치: 파이썬 전체 코드의 가장 아래쪽 main() 호출 부분

---

## 2차 Gemini 활용

- 사용 날짜:2차시
- 사용 목적:초기에 작성한 계획서(문서)와 실제 완성된 코드가 일치하는지 점검 및 수정
- 내가 입력한 프롬프트:이게 이 코드랑 같아? (계획서 내용과 코드 전체 붙여넣기)

- Gemini가 제안한 내용 요약:계획서와 코드의 핵심 흐름은 같지만, 변수명(weekly_habits ↔ eco_records)과 함수명(calculate_score ↔ analyze_eco_data)이 다르고, 계획서에 있던 취약점 분석 기능이 코드에는 빠져 있다는 차이점을 분석해 줌.
- 내가 반영한 부분:문서와 코드 중 하나를 선택해 통일하라는 조언을 받아들여, 계획서와 코드의 내용이 일치하도록 변수명과 함수명의 차이를 파악하고 정비함.
- 내가 수정하거나 사용하지 않은 부분:조건문을 활용해 가장 실천 횟수가 적은 취약 항목을 찾는 기능을 추가할 수 있도록 힌트를 주겠다고 했으나, 현재 작성된 print_report 중심의 출력 구조로도 충분하다고 판단하여 추가 로직은 삽입하지 않음.
- 반영 위치:프로젝트 계획서 문서(project_plan.md)의 4번(리스트 설계) 및 5번(함수 설계) 항목

---

## 3차 Gemini 활용

- 사용 날짜:3차시
- 사용 목적:완성된 코드를 바탕으로 문제 분해, 함수 작동 원리, 조건문/반복문 활용 등 논리적인 구조를 보고서에 글로 풀어내기 위한 도움
- 내가 입력한 프롬프트:조건문과 반복문 활용 써줘
테스트와 오류 수정 써줘


- Gemini가 제안한 내용 요약:작성된 코드의 흐름(입력-처리-출력)을 분석하여 보고서 양식의 각 항목에 들어갈 핵심 원리, 오류 원인 파악 내역, 조건문(if-elif)과 반복문(for)의 구체적인 사용 목적을 모범 문장으로 정리해 줌.
- 내가 반영한 부분:2차원 리스트 사용 이유(하루의 여러 데이터를 하나로 묶기), analyze_eco_data 함수의 누적 합산 및 가중치 로직 등 프로그램의 핵심 구조 설명을 보고서에 그대로 반영함.
- 내가 수정하거나 사용하지 않은 부분:AI가 작성해 준 모범 문장 구조를 참고하되, 최종 코드 인터뷰에서 내 입으로 직접 설명해야 하므로 내가 이해한 언어와 맥락에 맞게 문맥을 다듬어 제출함.
- 반영 위치:최종 보고서(report.md)의 전체 항목 (1번~9번)

---

## AI 활용 성찰

Gemini가 도움이 된 점:코드가 실행되지 않을 때 문법적 오류나 누락된 부분(main() 호출 등)의 원인을 빠르게 짚어주어 디버깅 시간을 단축할 수 있었다. 또한, 코드를 다 짠 후 이를 글로 설명하는 최종 보고서 작성 과정에서, 내가 작성한 코드의 논리 구조(입력-처리-출력, 2차원 리스트의 이점)를 더 명확하고 체계적으로 이해하는 데 큰 도움이 되었다.


Gemini의 제안을 그대로 쓰지 않고 내가 판단하거나 수정한 점:오류를 수정해 달라고 전체 코드를 통째로 넘겼을 때, AI가 정답 코드를 한 번에 주지 않고 구조나 빈칸을 채우는 방식으로 유도했다. 덕분에 리스트명이나 변수(append 기능 등)를 어떻게 결합할지 직접 고민해야 했고, 내 프로젝트의 원래 기획 의도에 맞춰 동적 입력 기능보다 고정 데이터 방식을 선택하는 등 스스로 방향을 판단하여 코드를 마무리했다.


다음에 AI를 사용할 때 주의해야 할 점:AI에게 단순히 "전부 다 고쳐줘"나 "빈칸 없이 다 써줘"라고 요구하기보다, 내가 먼저 짠 코드와 생각을 바탕으로 "이 부분의 로직이 왜 작동하지 않을까?", "이 원리를 보고서에 어떻게 설명하면 좋을까?"처럼 구체적으로 질문해야 진짜 내 실력이 된다는 점을 깨달았다. 무작정 복사하기보다는 내가 코드를 완벽히 이해하고 주도권을 가지는 것이 중요하다.

