# 최종 보고서

## 1. 프로그램 주제와 목적

내 프로그램의 주제:주일간의 친환경 실천 기록을 분석하는 '친환경 생활 습관 점수 판정기' 이다.

이 프로그램을 만든 이유:평소 친환경 실천이 중요하다고 생각하지만, 내가 얼마나 잘하고 있는지 객관적으로 알기 어려웠다. 그래서 요일별 실천 데이터를 바탕으로 가중치를 적용한 총점을 계산하고, 최종 친환경 실천 등급을 판정해 주는 프로그램을 만들게 되었다.


---

## 2. 문제 분해 및 모델링

### 입력
프로그램을 실행한 사람의 이름을 input() 함수를 통해 키보드로 입력받음.
실천 기록 데이터: 요일별 텀블러 사용, 분리배출, 대중교통 이용 여부가 기록된 7일 치 2차원 리스트 데이터(eco_records).
### 처리
항목별 총합 계산: 반복문(for)을 사용하여 2차원 리스트를 순회하며 텀블러, 분리배출, 대중교통 이용 횟수의 누적 총합을 각각 계산함.

가중치 총점 산출: 계산된 각 항목 총합에 정해진 가중치(텀블러 5점, 분리배출 3점, 대중교통 4점)를 곱한 후 모두 더하여 최종 총점을 구함.

### 출력
안내 문구: 프로그램 시작을 알리는 소개 텍스트 및 테두리 장식 출력.

최종 분석 결과: 사용자의 닉네임과 함께 계산된 이번 주 총점, 판정된 최종 등급, 그리고 3가지 항목별 실천 총합 횟수를 화면에 출력함.

---

## 3. 사용한 2차원 리스트 설명

2차원 리스트 이름:eco_records

한 행의 의미:요일별(하루 동안) 친환경 습관 실천 기록

각 열의 의미:


| 열 번호 | 의미 | 예시 |
|---:|---|---|
| 0번 열 | 요일 이름 | "월요일" |
| 1번 열 | 텀블러 사용 여부 | 1 (실천함) / 0 (안 함) |
| 2번 열 | 분리배출 수행 여부 |1 (실천함) / 0 (안 함)  |
| 3번 열 |대중교통 이용 여부  | 1 (실천함) / 0 (안 함) |

이 2차원 리스트를 사용한 이유:요일별로 3가지 실천 항목이 묶여 있는 복잡한 데이터를 다뤄야 했다. 2차원 리스트를 사용하면 하나의 행(row)으로 하루(요일) 동안의 여러 실천 항목을 한 번에 묶어서 처리할 수 있고, 반복문을 통해 쉽게 꺼내서 계산할 수 있기 때문이다.


---

## 4. 함수 설명

함수 이름    |               역할           | 입력값       | 반환값 또는 출력
analyze_eco_data | 2차원 리스트를 받아 항목별 |  weekly_habits | total_score (정수 점수)
(habits)        |  가중치를 적용해 총점 계산  |   (2차원 리스트) |
evaluate_grade(score)| 점수에 따라 친환경 등급을 판정 | score (정수 점수) | "A등급", "B등급" 등 (문자열)
print_report(habits) | 가장 실천 횟수가 적은 항목을 찾아 피드백 제공 | weekly_habits (2차원 리스트) | 없음 (화면에 피드백 출력)

가장 중요한 함수 하나를 골라 자세히 설명하세요.

함수 이름:analyze_eco_data(data)

설명:이 함수는 프로그램의 핵심 계산을 담당한다. for row in data: 반복문을 통해 2차원 리스트를 탐색하며 인덱스 [1], [2], [3]에 있는 값들을 각각 누적하여 합산한다. 이후 각 항목에 정해진 가중치(텀블러 5점, 분리배출 3점, 대중교통 4점)를 곱하여 총점을 구하고, 계산된 점수와 항목별 총합 리스트를 반환한다.


---

## 5. 조건문과 반복문 활용

### 조건문 사용 부분

evaluate_grade(score) 함수에서 if와 elif를 사용하여 사용자의 총점(score)이 특정 점수 구간에 해당하는지 검사했습니다. 총점이 60점 이상(score >= 60)이면 '최우수 지구 지킴이', 40점 이상(score >= 40)이면 '우수 실천가', 그 미만(else)이면 '조금 더 노력이 필요해요' 문구를 반환하도록 조건을 나누었습니다.


### 반복문 사용 부분

analyze_eco_data(data) 함수에서 for row in data: 형태의 반복문을 사용했습니다. 이를 통해 2차원 리스트(eco_records)에 담긴 일주일(7일) 치 행(row) 데이터를 첫 번째 줄부터 마지막 줄까지 순서대로 하나씩 꺼내오는 작업을 반복하며, 각 요일의 텀블러, 분리배출, 대중교통 이용 횟수를 누적해서 더하도록 처리했습니다.


### 조건문과 반복문이 함께 작동한 부분


---두 기능이 한 줄에 겹쳐서(중첩되어) 사용되지는 않았지만, 프로그램의 전체 흐름 속에서 유기적으로 연결되어 작동했습니다. 먼저 analyze_eco_data 함수에서 반복문을 통해 일주일 치 데이터를 모두 합산하여 최종 총점(total_score)을 만들어 냅니다. 그런 다음, 이 반복문이 만들어낸 총점 결과를 evaluate_grade 함수의 조건문으로 넘겨받아 최종 친환경 등급을 판정하는 순서로 프로그램이 완성하었습니다.

## 6. 실행 결과

대표 실행 결과를 붙여넣으세요.

```------------------------------
@20312moseungjae ➜ /workspaces/i-8-python-project-20312moseungjae (main) $ /usr/local/bin/python /workspaces/i-8-python-project-20312moseungjae/main.py
========================================
 친환경 생활 습관 점수 판정기 
일주일간의 실천 기록을 분석해 드립니다.
========================================
결과를 확인할 닉네임을 입력하세요: 김광교

[친환경 실천 분석 결과]
김광교님의 이번 주 총점은 45점입니다.
최종 등급: 우수 실천가 
------------------------------
텀블러 사용 총합: 4회
분리배출 수행 총합: 3회
대중교통 이용 총합: 4회
------------------------------

```

결과 분석:김광교는 총점이 45점이고 텀블러 사용 총합 4회 분리배출 수행 총합 3회 대중교통 이용 총합 4회를 하였다.


---

## 7. 테스트와 오류 수정

### 테스트 1: 정상 사례

입력:코드 내 eco_records를 기본 상태로 두고, 닉네임 입력창에 "환경지킴이"라고 입력함

예상 결과:텀블러 총 4회, 분리배출 3회, 대중교통 4회 합산) 텀블러(4×5=20) + 분리배출(3×3=9) + 대중교통(4×4=16) = 총점 45점. 40점 이상이므로 최종 등급은 "우수 실천가 "가 출력될 것으로 예상함.

실제 결과:예상과 완벽하게 동일하게 닉네임, 45점, "우수 실천가 " 등급, 그리고 각 항목별 총합 횟수가 화면에 출력됨.


### 테스트 2: 예외 또는 경계 사례

입력:: 코드 내 eco_records의 모든 숫자를 0으로 강제로 변경(미실천 상태)한 뒤, 프로그램 실행 후 닉네임 입력.

예상 결과:모든 실천 횟수가 0이므로 총점 0점, 40점 미만 조건인 else에 걸려 "조금 더 노력이 필요해요 " 등급이 출력될 것으로 예상함.

실제 결과:예상대로 총점 0점과 "조금 더 노력이 필요해요 " 등급이 정상적으로 출력됨을 확인함.


### 발견한 오류와 수정한 방법


---처음 코드를 짤 때 모든 데이터와 함수(show_intro, analyze_eco_data, evaluate_grade 등)를 완벽하게 정의했지만, 실행했을 때 화면에 아무것도 출력되지 않고 바로 종료되는 오류가 있었다. 이는 파이썬에서 함수를 정의만 하고 실제로 실행하라는 명령을 내리지 않아서 발생한 논리적 오류였다. 코드 가장 밑바닥에 main() 이라는 함수 호출 코드를 딱 한 줄 추가하여 프로그램이 정상적으로 작동하도록 수정했다.

## 8. Gemini 활용 내용 요약

Gemini에게 도움받은 부분:emini에게 도움받은 부분:
전체적인 프로그램의 뼈대를 잡고, 각 함수가 어떻게 연결되어야 하는지(입력-처리-출력 흐름)에 대한 구조적 도움을 받았다. 또한 작성한 코드가 실행되지 않을 때 main() 함수 호출이 누락되었다는 원인을 파악하는 데 도움을 얻었고, 2차원 리스트와 코드의 변수명이 계획서와 달랐던 부분을 점검받았다.


Gemini가 제안했지만 내가 수정하거나 사용하지 않은 부분:Gemini가 프로그램을 실행할 때마다 사용자가 요일별 횟수를 직접 input()으로 입력받아 동적으로 리스트를 생성하는 방식을 제안했다. 하지만 현재 나의 프로젝트 계획서와 수행평가 기준에 맞춰, 우선은 코드 내에 2차원 리스트 데이터를 미리 작성해 두는(하드코딩) 방식을 유지하기로 결정하고 그 제안은 사용하지 않았다.


최종 코드에서 내가 직접 판단하거나 수정한 부분:프로그램 시작 시 출력되는 인사말 문구, 텀블러/분리배출/대중교통 점수에 곱해지는 가중치(5, 3, 4) 설정, 그리고 최종 등급을 나누는 기준 점수(60점, 40점)와 각 구간의 등급 이름("최우수 지구 지킴이", "우수 실천가" 등)은 내 프로젝트 기획 의도에 맞게 내가 직접 수치와 문자열을 판단하고 수정하여 완성했다.


---

## 9. 한계와 개선 방안

현재 프로그램의 한계:현재 프로그램은 eco_records라는 데이터가 코드 안에 고정되어 있어서 닉네임을 다르게 쳐도 프로그램을 실행할 때마다 항상 똑같은 결과가 나온다.


다시 개선한다면 추가하고 싶은 기능:, 프로그램이 시작될 때마다 사용자에게 요일별 실천 횟수를 input()으로 직접 질문하여 입력받도록 수정하여, 매번 사용자마다 다른 결과를 분석할 수 있도록 코드를 업그레이드하고 싶다.


---

## 10. 코드인터뷰 준비

다음 질문에 답할 수 있도록 준비하세요.

1. 내 프로그램의 입력-처리-출력은 무엇인가?
2. 2차원 리스트에서 한 행과 각 열은 무엇을 의미하는가?
3. 가장 중요한 함수는 무엇이고 어떤 역할을 하는가?
4. 조건문과 반복문은 어디에서 사용되었는가?
5. Gemini가 도와준 부분과 내가 직접 수정한 부분은 무엇인가?
